'use strict';


// Declare app level module which depends on filters, and services
angular.module('kontrolyApp', [
  'ngRoute',
  'kontrolyApp.filters',
  'kontrolyApp.services',
  'kontrolyApp.directives',
  'kontrolyApp.controllers'
])
